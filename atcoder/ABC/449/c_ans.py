import io
import sys
from collections import Counter

# 下記に標準入力を記載
_InPUT = """\
6 2 4
aabcba
"""
sys.stdin = io.StringIO(_InPUT)
# ここからコードを記載
"""
#右端の文字を1つずつ見ながら、その左側で「ちょうど条件を満たす範囲」にある文字数を管理して数えているコード

#つまり、各 j について
j より左にあって距離が L 以上 R 以下で
しかも同じ文字の i を何個持つか数えればよい

たとえば L=2, R=4 なら、j から見て
2文字前
3文字前
4文字前
にある文字だけ見ればよい

L ≤ j - i ≤ R
これは左と右の距離が，L以上R以下という意味

たとえば L=2, R=4 なら，
距離 1 はダメ
距離 2 はOK
距離 3 はOK
距離 4 はOK
距離 5 はダメ


i ≤ jは左の位置から右の位置を見るというだけ
たとえば (2,6) は数えるけど，(6,2) は数えない
"""

n, l, r = map(int, input().split())
s = input()

"""
本来ほしい範囲は[j-R, j-L]
これを0-index で i を左から順に見ながら管理すると、
i-l の文字が新しく範囲に入る
i-(r+1) の文字が範囲から外れる
という形になる

だから先に
r += 1しておくと、

if i >= r:
    cnt[s[i-r]] を引く

で書ける
"""
r += 1

ans = 0
cnt = [0] * 26  # 各文字a-zそれぞれの個数を管理

#このiは今、右側の文字 S[j] として見ている位置(0-indexより問題文の j=1 はコードでは i=0、問題文の j=2 はコードでは i=1)
for i in range(n):
    # 新しく範囲に入る文字を追加
    if i >= l:
        cnt[ord(s[i - l]) - ord('a')] += 1

    # 範囲から外れる文字を削除
    if i >= r:
        cnt[ord(s[i - r]) - ord('a')] -= 1

    # 今見ている s[i] と同じ文字が何個あるか足す
    ans += cnt[ord(s[i]) - ord('a')]

print(ans)

"""
#常に|L-R|の文字列に関するカウント情報のみ保持するようにして、計算を最小限にした

大丈夫です。
ここは**「なぜ足して、なぜ引くのか」**が急に出てきて，かなり混乱しやすいところです。

いったん式を忘れて，**「右端 `i` を1つずつ右にずらしたとき，左側の見る範囲がどう動くか」**だけ見ます。

---

## まず，何を管理したいのか

今，右端を `i` として見ているとき，左側で見たい位置は

```text
i-R から i-L
```

です。

たとえば `L=2, R=4` なら，

```text
i-4, i-3, i-2
```

の3か所だけ見ればいいです。

---

## 具体例で見る

文字列はひとまず無視して，位置番号だけ見ます。

`L=2, R=4` とします。

### 右端が `i=4` のとき

見たい左側は

```text
4-4, 4-3, 4-2
= 0, 1, 2
```

つまり **0,1,2番目** の文字を見たいです。

---

### 次に右端が `i=5` のとき

見たい左側は

```text
5-4, 5-3, 5-2
= 1, 2, 3
```

つまり **1,2,3番目** の文字を見たいです。

---

## ここで何が起きた？

`i=4` のときの範囲は

```text
0,1,2
```

`i=5` のときの範囲は

```text
1,2,3
```

になりました。

比べると，

* **新しく 3 が入った**
* **0 が外れた**

だけです。

全部を毎回数え直さなくても，

* 入ってきたものを足す
* 出ていったものを引く

だけで済みます。

これがコードの核心です。

---

# 1つ目の式

```python
if i >= l:
    cnt[ord(s[i - l]) - ord('a')] += 1
```

これは

**新しく範囲に入ってきた文字を足している**

という意味です。

---

## なぜ `i-l` が入ってくるの？

見たい範囲は

```text
i-R から i-L
```

でした。

この中で一番右の位置は

```text
i-L
```

です。

つまり，右端 `i` を1つ進めたときに，新しく右側から範囲に入ってくるのが `i-L` の位置です。

---

## 例で見る

`L=2, R=4` のとき，

* `i=4` なら範囲は `0,1,2`
* `i=5` なら範囲は `1,2,3`

でした。

このとき新しく入るのは **3** です。

そして

```text
i-L = 5-2 = 3
```

ですよね。

だから `i-l` を足しているのです。

---

# 2つ目の式

```python
if i >= r:
    cnt[ord(s[i - r]) - ord('a')] -= 1
```

これは

**範囲から外れた文字を引いている**

という意味です。

---

## でも，なぜ `i-r` なの？

本当は「距離 `R` までOK」なので，
**距離 `R+1` のものはダメ**です。

コードでは最初に

```python
r += 1
```

しているので，この `r` はもう **`R+1`** になっています。

だから

```text
i-r
```

は

**右端 `i` から見て，距離 `R+1` の位置**

です。

そこはもう遠すぎるので，範囲から外します。

---

## 例で見る

さっきと同じく `L=2, R=4` です。

コードでは最初に `r += 1` しているので，

```text
r = 5
```

になっています。

`i=5` のとき，

```text
i-r = 5-5 = 0
```

です。

これはちょうど，さっき範囲 `0,1,2` から `1,2,3` に変わるときに
**外れた 0** です。

だから `i-r` を引いているのです。

---

# つまり毎回やっていることはこれだけ

右端 `i` が1つ進むと，左側の見る範囲も1つ右にずれます。

たとえば

```text
0,1,2  →  1,2,3
```

みたいに動きます。

そのとき

* 新しく入る 3 を足す
* いらなくなった 0 を引く

これを式で書くと

```python
cnt[s[i-l]] を足す
cnt[s[i-r]] を引く
```

になります。

---

# `cnt` は何を表していたのか

`cnt` は，

**今見ている範囲の中に，各文字が何個あるか**

を持っています。

たとえば今の範囲が

```text
a, a, b
```

なら

* `a` は2個
* `b` は1個
* それ以外は0個

です。

だから，今の右端の文字が `b` なら，

「左側の範囲に `b` が何個あるか」を見れば，そのまま答えに足せます。

---

# 具体的に `aabcba` で見る

`S = aabcba`, `L=2`, `R=4` です。
コードでは `r += 1` するので，内部では `r=5` として考えます。

---

## `i=4` のとき

見たい位置は

```text
0,1,2
```

文字は

```text
a,a,b
```

です。

だから `cnt` は

```text
aが2個, bが1個
```

になっています。

今の右端 `s[4]` は `b` なので，
左側範囲にある `b` の数 1 を答えに足します。

---

## `i=5` に進むとき

見たい位置は

```text
1,2,3
```

に変わります。

つまり

* 位置3の文字 `c` が新しく入る
* 位置0の文字 `a` が外れる

です。

これをコードでやると

```python
cnt[s[5-2]] += 1   # s[3] を足す
cnt[s[5-5]] -= 1   # s[0] を引く
```

です。

まさに

* `i-l` を足す
* `i-r` を引く

になっています。

---

# 超短く一言で言うと

この2行は，

**「左側で今見るべき範囲」を1個右にずらしたときの差分更新**

です。

* `i-l` は，新しく範囲に入る位置
* `i-r` は，範囲から出ていく位置

です。

---

# まだ一番引っかかりやすいところ

多くの人は

> なんで `i-l` を足すの？
> なんで `i-r` を引くの？

ではなく，本当は

> **今，どの範囲を管理しているの？**

が曖昧で混乱しています。

答えは，

**右端 `i` に対して，左側の `i-R` から `i-L` までの範囲**
です。

この範囲が `i` の増加とともに

```text
0,1,2 → 1,2,3 → 2,3,4 → ...
```

のようにスライドするから，

* 右から入るものを足す
* 左から出るものを引く

だけで済むのです。

---

必要なら次は，`aabcba` について
**`i=0` から `i=5` まで，毎回「範囲」「足す文字」「引く文字」「cnt」「ans」** を表で全部書きます。
"""